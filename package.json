{
  "scripts": {
    "install": "npm install --prefix server",
    "start": "npm start --prefix server",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
